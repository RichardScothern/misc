# misc

Collect timings around throughput of docker image downloads.  An implementation of the Docker Registry serving content via the Fastly CDN (versus CloudFront) was used and performance percentiles collected.  

Fastly is fast.
